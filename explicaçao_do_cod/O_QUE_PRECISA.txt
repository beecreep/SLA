##ANTES DE TUDO FAÇA O .VENV FUNCIONAR E INSTALE AS DEPENDENCIAS

#pra remover o venv atual
rm -rf .venv #(recursive force)

#para criar um .venv de acordo com o seu SO
python -m venv .venv
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt


pip install flask-migrate


#se tiver o .venv
source .venv/Scripts/activate  # ou bin/activate se for Linux

#Força instalação do pip no venv
python -m ensurepip --upgrade 

sudo apt update && sudo apt upgrade -y
sudo apt install python3 python3-pip python3-venv

#arquivo com uma lista de (dependências) pacotes necessários e a versão pra rodar o projeto
#são impoirtantes pra não aparecer modulenotfounderror no terminal, toda vez que tentar o python app.py
pip freeze > requirements.txt


#instala o que está em requirements.txt
pip install -r requirements.txt

#faz o oposto do que está acima
pip uninstall -r requirements.txt -y 
#r diz para read o arquivo, y de yes

#mas você pode fazer tudo iss apenas desinstalando o venv


pip install flask-login
pip install python-dotenv


#RESUMO DO PROJETO 
python app.py

Projeto Flask:

Roda do servidor,

Interface no navegador,

Integração com banco,

Ou quer deixar o front mais bonitinho,



